{
  "name": "fateme-abbasi-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Bilingual (English/Persian) static portfolio for Fateme \"Mona\" Abbasi.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:studio": "npm --prefix monaabbasi/studio run build && node scripts/place-admin.mjs",
    "build:all": "npm run build && npm run build:studio",
    "clean": "node -e \"import('node:fs').then(fs=>fs.rmSync('dist',{recursive:true,force:true}))\"",
    "serve": "node scripts/serve.mjs",
    "smoke": "node scripts/smoke.mjs"
  },
  "config": {
    "siteUrl": "https://monaabbasi.netlify.app"
  },
  "devDependencies": {
    "jsdom": "^24.0.0",
    "playwright": "^1.61.0"
  },
  "dependencies": {
    "marked": "^18.0.5"
  }
}
